MI5.ai — Windows setup

1. npm install
2. npm start   (or double-click "Launch MI5.bat" after step 1)

GROK KEY READY: Select "Grok (xAI)" → paste your gsk_ key → Save

Files included:
- main.js (Electron shell, IPC, Grok API)
- app.html (dashboard UI)
- overlay.html (live HUD overlay; content protection hides HUD from most screen captures on supported Windows/macOS)
